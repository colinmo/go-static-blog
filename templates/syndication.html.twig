<p class="syndication-links">{% if links|length > 0 %}Syndication: {% for label, url in links %}[<a href="{{ url }}" class="u-syndication">{{ label }}</a><a href="https://brid.gy/publish/{{ label|lower }}"></a>] {% endfor %}{% endif %}</p>